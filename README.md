# Role Memory Forge

给 SillyTavern 用的角色扮演记忆插件：自动把每轮 `{{user}}` 和角色回复压缩成简记，按层级生成阶段总结和大总结，并实时维护角色状态、人物档案、人物关系、世界设定、物品栏、约定、当前剧情与发展方向。

## 0.3.2 新增

- 参考「ACU 星数据库 III」的数据库式面板逻辑，把原来的文字 Markdown 关系表改成 **RMF Index 格式**。
- 世界书新增 `[RMF] 03 RMF_INDEX_DATABASE` 和 `[RMF] 04 状态档案设定表`。
- 人物关系、关系图节点、关系图连线、角色状态、人物档案、世界设定、物品栏、约定、剧情进度、简记流水、阶段总结都会以 `RMF_INDEX_TABLE / JSON_2D_ARRAY` 保存。
- 悬浮球面板新增「Index 数据表」视图，不再只显示文字表格。
- 模型提示词已改成：不要求 AI 输出 Markdown 表格，只输出结构化 `relationships` 数组，由插件自动转成 Index 表。
- 保留关系网络图、折叠简记、新聊天保留记忆开关。
- 默认同步目标改为 **角色卡自带世界书**，不再自动新开一个 RMF 世界书。
- 新增「自动同步到世界书」开关：每次 AI 回复后完成填表，就会立刻写回世界书。
- 修复手机/窄屏/部分主题下按钮和文字被挤成竖排的问题。

## RMF Index 格式示例

插件写入世界书时会使用这种格式：

```text
<RMF_INDEX_TABLE name="人物关系表" mode="JSON_2D_ARRAY" updated="2026-07-06T00:00:00.000Z">
[
  ["序号", "人物A", "人物B", "关系", "当前状态", "张力值", "证据"],
  ["1", "花木兰", "兰陵王", "短暂的队友", "并肩作战但互相试探", "35", "两人在本轮共同应对敌人"]
]
</RMF_INDEX_TABLE>
```

第一行是字段，后面每一行是数据。这样比 Markdown 表格更稳定，也更方便以后接别的数据库插件、向量召回或脚本读取。

## 核心功能

1. 每次 user 和 AI 完成一轮对话后，自动生成一条简略记录。
2. 默认每 20 条简记生成一次阶段总结。
3. 默认 5 个阶段总结合并成一次大总结。
4. 每次生成前自动注入：大总结 + 大总结之后的阶段总结 + 最近简记 + Index 人物关系表 + 状态表。
5. 实时维护：角色状态、人物档案、人物关系、世界设定、物品栏、约定、当前剧情、发展方向。
6. 默认直接写入角色卡自带世界书；找不到时可切换到“当前选中的世界书”或“自定义 RMF 世界书”。
7. 支持使用 SillyTavern 当前 API，也支持自填 OpenAI-compatible API 地址、Key、模型名。
8. 支持新聊天清空或保留当前角色旧记忆。

## 安装

1. 把本仓库上传到 GitHub，保证 `manifest.json` 在仓库根目录。
2. 打开 SillyTavern。
3. 进入 `Extensions` → `Install Extension`。
4. 粘贴你的 GitHub 仓库地址。
5. 安装后刷新 SillyTavern。
6. 点击右下角 🧠 悬浮球，启用插件。

## 使用建议

### 总结来源

- **使用 SillyTavern 当前 API**：最省事，推荐先用这个。
- **自填 OpenAI-compatible**：适合你想单独用一个便宜模型做记忆总结。填写格式一般是：
  - API 地址：`https://api.xxx.com/v1` 或 `http://127.0.0.1:8000/v1`
  - API Key：你的密钥
  - 模型名：例如 `deepseek-chat`、`gpt-4o-mini`、`Qwen...`

### 折叠简记

开启后，AI 每次回复末尾会自动追加：

```html
<details>
  <summary>🧠 本轮记忆简记 #1</summary>
  ...简记内容...
</details>
```

若你不想让聊天正文变长，可以在悬浮面板里关闭「回复结尾折叠简记」。

### 世界书条目

默认情况下，插件不会再新建世界书，而是直接写入当前角色卡已经绑定的世界书。你也可以在面板里切换同步目标：

```text
角色卡自带世界书（推荐，不新建）
当前选中的世界书（不新建）
自定义 RMF 世界书（允许新建）
```

插件会写入这些 RMF 专用条目：

```text
[RMF] 00 当前记忆总览
[RMF] 01 每层简记流水
[RMF] 02 可视化关系表
[RMF] 03 RMF_INDEX_DATABASE
[RMF] 04 状态档案设定表
[RMF] 99 JSON_RAW_DO_NOT_EDIT
```

`JSON_RAW` 用来在「新聊天保留记忆」时恢复状态，不建议手动改。

## 向量化要不要加？

当前版本仍然不强制向量化。你的需求更偏「确定性分层总结 + 每次注入总结」，先保证稳定、直观、可控。以后聊天特别长、角色很多、需要从几千层前按语义召回某个细节时，再加向量化更合适。

## 注意

- 如果你编辑或删除旧消息，建议点击「补录当前聊天」，让记忆重新生成。
- 浏览器直连部分 API 可能会被 CORS 拦截，这种情况优先使用 SillyTavern 当前 API。
- 本插件会写入当前聊天 metadata 和角色记忆世界书。


## v0.3.1 修复说明

- 修复：部分 SillyTavern 版本里静态 import 失败会导致插件只在“管理扩展程序”里显示，但没有悬浮球、没有设置入口。
- 新增：扩展设置面板内固定入口“Role Memory Forge”，即使悬浮球被主题挡住，也可以从这里打开记忆面板。
- 强化：悬浮球使用最高层级 z-index，并增加手机端 safe-area 适配。
- 强化：启动时先渲染 UI，再动态加载 WorldInfo / SillyTavern API；即使世界书 API 暂时失败，面板也不会消失。
- 强化：事件监听失败时启用兜底轮询，AI 回复后仍会自动补录最新一轮。


## v0.4.0 修复说明

- 默认改为自动同步到角色卡自带世界书。
- 不再默认新开 `RMF-角色名-记忆世界书`；只有你主动选择“自定义 RMF 世界书”或打开“找不到角色卡世界书时允许新建”才会新建。
- 面板新增“世界书同步目标”选择。
- 修复底部按钮、开关、输入框在窄屏下竖排和错位的问题。
- 扩展入口状态栏会显示当前同步目标。


## v0.4.0 新增

- 新增“一键记忆历史聊天记录”：导入别人角色卡的旧聊天记录后，可以从 0 层到当前逐轮补录为插件记忆。
- 新增“走马灯回顾”：点击后模型会整理当前角色卡从开头到现在的详细剧情回放，并可作为 AI 回复写入聊天，用于测试记忆是否有效。
- 走马灯内容会同步进角色卡自带世界书的 `[RMF] 05 走马灯回顾` 条目。
- 面板增加历史补录/走马灯设置：每段处理轮数、最多回顾轮数、是否写入聊天。
